[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    ui: UI/UX and template structure tests
    integration: Route-level integration tests
    unit: Pure unit tests (helpers, db utils)
